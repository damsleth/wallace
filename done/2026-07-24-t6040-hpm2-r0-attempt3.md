# T6040 right-HPM2 R0 attempt 3: inactive endpoint requires WAKEUP

Date: 2026-07-24
Ticket: 093
Result: **BOUNDED STOP — selector command ACKed; endpoint remained inactive**

## Exact artifact and transcript

```text
m1n1 commit:
  471700035efd99e2e0bf4c3f5d6fbccaf07beb32
R0 m1n1.bin:
  3a8af1e7daa65a431486f9cebbcad87f72995af5af601695ba93673710a2f01a
chainload transcript:
  /Users/damsleth/Code/linux-build-out/hpm2-r0-attempt3-chainload-20260724.log
  aa982f7926b1e990d6f77622cfbd78bc5e5d4c448d1f19b38345e3fa980606ef
```

The rig lease was held by `sol`. The exact artifact hash and independent
source/build review passed before upload.

## Observed boundary

The identity gate passed, including complete raw and translated controller
tuples. The approved Register-0 selector transaction ACKed, and the bounded
read of the physical selector window returned zero:

```text
t6040-hpm2: ADT identity PASS, direct endpoint /arm-io/nub-spmi-a1/hpm2 sid=0x0c
t6040-hpm2: logical-select reg=0x20
spmi-strict: TX sid=0x0c opc=0xa0 extra=0x2000 in=0 out=0
spmi-strict: RX header=0x00008ca0
spmi-strict: RX validated data
spmi-strict: TX sid=0x0c opc=0x20 extra=0x0000 in=0 out=1
spmi-strict: RX header=0x00010c20
spmi-strict: RX data-word=0x00000000
spmi-strict: RX validated data 00
t6040-hpm2: unexpected selector value 0x00
t6040-hpm2: class R0 FAIL; intentional stop and warm reboot
```

No logical data-window read occurred. There was no WAKEUP, SSPS, extended
write, IRQ/event access, USB/PHY/storage path, SError, or retry. The automatic
warm reboot ran, and the standard DebugUSB recovery returned to a healthy
quiescent proxy before the lease was released healthy.

## Interpretation

The exact public `tps6598x-spmi` initialization sequence sends SPMI WAKEUP and
waits 10 ms before its first selector command. R0 deliberately omitted WAKEUP
to test the narrower boundary. Its ACK plus zero selector read demonstrates
that the controller and SID respond while the SN201202x register window
remains inactive; it does not disprove the selector encoding.

Ticket 093 is complete as a bounded inactive-endpoint result. It must not be
retried unchanged.

## Next experiment boundary

Ticket 094 is narrowed to the minimal missing prefix:

1. reassert the exact identity gate;
2. send one bounded WAKEUP to SID `0x0c`;
3. wait exactly 10 ms;
4. issue the same selector and one-byte logical power-state read;
5. stop and warm reboot.

The revised R1 contains no logical data write, SSPS, SLEEP, SHUTDOWN, RESET,
IRQ/event access, USB, or PHY path. This is a strict subset of ticket 094's
maintainer-approved WAKEUP+conditional-SSPS scope. It requires new exact
artifacts and independent review before one live attempt.
