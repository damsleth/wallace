# Cross-review: ticket 002 op-115 read-only isolation (PASS, one precondition)

Reviewer: `fable`, 2026-07-14. Author: `claude`
(`done/2026-07-14-t6040-pcie-op115-static-analysis.md`). Reviewed against
`~/Code/m1n1/AGENTS.md` non-negotiables per COORDINATION §Cross-agent review.
All claims verified against the artifacts.

## Verified

- **Main binary**: `~/Code/m1n1/build/m1n1.bin` at `d1494f5a` recomputes to
  `5616b05f…adef67` ✓ (matches the packet).
- **Curated parity**: `src/pcie.c` and `src/tunables.c` at curated `c5d9de6c`
  are byte-identical to main `d1494f5a` ✓.
- **Manifest**: `done/2026-07-14-t6040-pcie-op115-read-manifest.tsv` recomputes
  to `4f377fad…67524` ✓; regenerated byte-exact from the committed J614s ADT
  (`87f5c391…98195` ✓) via `t6040-pcie-write-plan.py --isolate-op115-read` ✓.
  Operations 1–114 are row-identical to the live-proven shared-PHY prefix
  (only the two header comment lines differ, as expected); the sole new
  operation is `115 … 0x417040090 4 READ` ✓.
- **Code** (`src/tunables.c: tunables_read_first_local_addr_trace` +
  `src/pcie.c` gate): T6040-only (`state->pcie_regs == &regs_t6040`); address =
  ADT `reg[3]` base + first `apcie-phy-ip-pll-tunables` entry offset — nothing
  invented; width selected from the ADT entry (4); `dsb sy` + read-only
  `L2C_ERR_STS` sample before and after, aborting without clearing status;
  prints the value; **returns unconditionally before the write** — operations
  116–351, ports, PERST#, RID2SID/MSIMAP, config space, Linux PCIe, NVMe, and
  storage are unreachable. The `-1` return path from `pcie_init_controller`
  is the same stop mechanism the guarded-clock run already live-proved (it
  booted Linux afterward).
- **Static evidence**: the packet's Apple-side claim (both paired kernelcaches
  RMW PHY-IP offset `0x90` via 32-bit I/O on the reg[3] aperture, after the
  same two tunable objects) is internally consistent and proposes no new
  ordering, poll, or offset. The read-only split is the minimal discriminating
  experiment, with a pre-registered interpretation.
- **Kernel artifacts**: `Image` `14da8640…3abaf4` ✓ and
  `initramfs-dcuart.cpio.gz` `512c69da…c317ca` ✓ still match their pins.

## Finding: the pinned base DTB no longer exists on disk

The packet (like the four prior PCIe diagnostics) pins the PCIe-free base DTB
by hash `e7691ee4…3dc9a` only — no filename was ever recorded. A full search
of `~/Code` and `/private/tmp` finds **no file with that hash**;
`t6040-j614s-dcuart.dtb` in `~/Code/linux-build-out` now hashes
`b3858f60…7814` (later rebuilds overwrote it). Before the live run, the runner
must either rebuild a DTB that reproduces `e7691ee4…` exactly, or produce a
fresh PCIe-free base DTB, decompile-verify it (no PCIe host node, NVMe/SART
disabled), record its hash, and have it cross-reviewed. Going forward, base
DTB pins should include the filename and a stashed copy, as was done for the
m1n1 control binary.

## Verdict

**PASS** on the m1n1 code, binary, and manifest — ready for CJ approval —
with the single precondition that the exact base DTB be restored or re-pinned
and reviewed before boot. One run only; interpret per the packet's
pre-registered matrix; any write-side follow-up needs a new manifest, review,
and approval.
