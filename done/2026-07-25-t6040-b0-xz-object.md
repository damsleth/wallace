# T6040 B0 XZ dual-mode object — 15.2 MiB (2026-07-25)

Response to the enrolled-boot loop of the 22 MB object `46237ade` (iBoot
enrolled-object size limit, unmeasured; probe ladder running). Same payload,
smaller wrapper.

```text
m1n1-b0-alpine-openrc-earlyproxy-xz.bin
SHA-256 4340ec37e7284b96991d09a178c78adba0ee6769645700eb00cb99dfa85fed50
size 15,945,580 B (15.2 MiB), entry 0x800
```

- m1n1 prefix: dual-mode `11f6fbf1` (EARLY_PROXY_TIMEOUT=5, ticket-119 reviewed)
- kernel: `Image-hid-type-fix` XZ member `cbb3e743` — **decompresses to exactly
  `df7657c1`**, the ticket-100 live-proven kernel
- DTB: `2782b922` unchanged; initramfs: OpenRC root XZ member `6ebc3e82` —
  decompresses to the exact proven bytes; bootargs `3659a0da` unchanged
- XZ members are minilzlib-compatible by construction: `xz -9e --check=crc32
  -T1` → single-stream, single-block, CRC32, no BCJ (constraints from
  `src/minilzlib/xzstream.c`)
- `t6040-build-raw-object.py` extended to accept pre-compressed XZ members
  (kernel + initramfs); strict verifier already supported XZ. Strict verify PASS.

Validation plan (separates the two risk variables):
1. **Tethered chainload of this exact object** — chainload exercises the same
   m1n1 payload parser + minilzlib XZ decode as the enrolled path; proves
   decompression + Alpine boot without touching enrollment.
2. **iBoot size budget** — the padded probe ladder (4/8/12/16/20 MiB bare-m1n1
   objects, maintainer-driven enroll + cold boot) measures the enrolled-path
   limit independently.
3. Both pass and 15.2 MiB fits the measured budget → enroll `4340ec37`.

Kernel-diet contingency if the limit lands below ~15.2 MiB: trimmed kernel
config (Image is 50.8 MiB raw; T6040-essentials config should cut it hard).
