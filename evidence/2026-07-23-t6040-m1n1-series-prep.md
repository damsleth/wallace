# T6040 m1n1 upstream series prep — ticket 046 (2026-07-23)

Audit of `~/Code/m1n1` (main, working/diagnostic) vs `~/Code/m1n1-clean`
(`t6040-bringup`, curated) to shape the T6040 upstream series. The two are
**divergent histories on a shared base** (last shared: `fd20d7f7` proxyclient
pmgr helper, `0ec216de` CPUSTART); clean is a re-curation, not a cherry-pick, so
the PCIe diagnostic commits carry different hashes in each.

## Source drift (working trees) — near-total convergence

Byte comparison of the 7 bring-up files: `cpufreq.c`, `mcc.c`, `kboot.c`,
`dapf.c`, `pmgr.c`, `wdt.c` are **identical**; only `pcie.c` differs — **+23
lines in main only**, the `e4671e08` clkgen-PLL block (`src/pcie.c:495-517`),
which is the diagnostic op-115 candidate and intentionally stays out of the
series. So every functional T6040 change except that probe is already in clean.

## Proposed upstream series (dependency-ordered, from clean)

| # | Commit | Subsystem | Upstream-readiness |
|---|---|---|---|
| 1 | `80b4f4c9` cpu: M4 park secondaries in WFE | cpu/smp | clean (foundational) |
| 2 | `52c1f6a8` wdt: add `wdt_arm_secs()` | wdt | clean (helper for 6b) |
| 3 | `a514f011` dapf: gate DAPF init on M4 gen | dapf | clean |
| 4 | `8eb9e2e7` mcc: t6041 support | mcc | clean (+119, self-contained) |
| 5 | `c009ef68` cpufreq: minimal t6040 | cpufreq | clean — **flag "intentionally partial"** |
| 6a | kboot: t6040 display carveout reuse | kboot | clean |
| 6b | kboot: arm 20s wdt before M4 handoff | kboot | **bring-up crutch — split out, mark temporary** (commit body: removable once the guest runs a wdt driver) |
| 7 | `cf11d707` pcie: t6040 reg map | pcie | clean (foundational for all PCIe) |
| 8 | `b52036e0` proxyclient: split PMGR policy from ADT topology | tooling | clean |

Note: commits 6a/6b currently live as one commit (`4dc497ec`) — **split for
upstream** so a maintainer can reject the wdt crutch independently.

## Exclude from the series (diagnostic / already-upstream)

- **Entire PCIe diagnostic series** (clean `df9765b0`…`f0738eee`: init clock+PLL,
  trace writes, bound/stage/fence/zero-write/dry-run, guarded clock diag, gate
  after shared PHY, op-115 read-only stop) — diagnostic-only.
- **`e4671e08`** clkgen-PLL enable+lock — its own message says "not for upstream
  as-is"; the only un-mirrored source delta. If wanted in clean for
  reproducibility, cherry-pick as a *diagnostic* commit, keep out of the series.
- **`66284c4f`** proxyclient `experiments/t6040.py` — experiments dir, exclude.
- **`0ec216de`** CPUSTART — authored by yuka (`yuka@yuka.dev`), **already
  upstream** (generic `0x88000 + die*0x20_0000_0000`); do NOT resubmit.
- **`fb5813a6`** kboot log-buffer top-of-RAM guard — **review separately**: may be
  a legitimate robustness fix, but confirm it isn't papering over the trace hack
  before proposing on its own merits.

## Net for the cover letter

Eight upstream-ready commits (split #6 into two; flag cpufreq-minimal and the
wdt-arm crutch for maintainer judgment), spanning cpu/wdt/dapf/mcc/cpufreq/
kboot/pcie-regmap + a proxyclient tooling fix. Exclude the PCIe diagnostic
series, the clkgen probe, the experiments helper, and yuka's already-upstream
CPUSTART. **Draft prep only — CJ posts.** Remaining 046 work: write the actual
cover letter + rebase-format the eight commits (a follow-on when CJ is ready to
submit); the analysis/selection is done here. Ticket 046 analysis done.
