# Ticket 096 — HPM2 class-10 detach/rollback: full static-decode record

Moved verbatim from `tickets/096-hpm2-class10-detach-rollback.json` on
2026-08-03 during the ticket-record cleanup (desc/note/progress journals
belong in `evidence/` per `tickets/README.md`). Nothing below is edited;
each block is the exact prior field content.

## Former `desc` field

STATIC RESULT 2026-07-25 (claude, fresh-attempt pass): STILL OPEN / R3 STILL NO-GO. Items 1, 2 and 6 are not all FOUND, so 096 does not close. Per-item: (1) VBUS-off NOT FOUND - the driver has no VBUS primitive in EITHER direction; setAccessoryPower is a kIOReturnUnsupported stub in both class families, no VBUS/role/source 4CC exists, no VBUS platform function exists, and Type10 turnOnVbus() is only fWaitingForUSB=false + stopUSBTimer + forcePortEvaluation (its caller is driverStatusInterrupt, i.e. it is the USB-host-driver-ready ack). (2) The 0x14 OR-mutation PREMISE IS VOID - the ORs land on a stack buffer / the software cache at this+0xf28 and are handed to processInterruptEvents (slot +0xb38); register 0x14 is never written anywhere in the kext. The cached/W1C ownership protocol IS now fully decoded (this+0xf28 cache, this+0xf66 valid, this+0xf59 defer gate). The 0x18 W1C consumption remains irreversible and provably has no inverse. (3) Mask/detect restoration NOT FOUND - setInterruptMask writes a synthesized constant 07 0d 00 00 02 {00|10} c0 {0b|8b} to 0x16, never reads 0x16, never saves the prior value; the real HAL RMW is unreachable from AppleTCController; no path restores a saved detect state. (4) Pre-SSPS 0x07 NOT FOUND - Type10-reachable SSPS args are exactly {0,2,3}; 0x07 is never passed and never appears as an SSPS payload; register 0x20 is read-only to the driver. (5) 0x23/0x24/0x55 NOT FOUND and OUT OF SCOPE - all four accesses live in HALType5 wrappers whose only six call sites are in Type16/Type17, which gate on hpm-class-type 16/17 and are unreachable from class 10; 0x24 is never written, 0x55 is never read, neutral 0x55 remains unproven. (6) Cross-layer teardown order NOT FOUND, structurally: Apple performs NO cross-layer teardown on detach - processModeFlags is a pure software object reconciler (reads 0x48/0x49, no writes), the eUSB2 repeaterReset ('EURr' via platform-function 4CC 'prst', payload domain {0,1,2}) has no caller anywhere in the corpus, eusb2phy_shutdown is known-off rather than a byte-preserving or even complete bit inverse of init, PHY teardown is driven by client refcount/close not cable detach, and xHCI orders against the PHY only on driver stop. NEW POSITIVE EVIDENCE: the HPM-layer detach/reattach order IS fully specified and bidirectional via AppleTCControllerType10::overrideHPMMode(enforce, keepRoleAndOrientation, dataStatus, status) @0xfffffe000952c3bc, reachable by IOKit message selector 0xe3ff843f with a decoded arg struct, and the pre-transition values are readable from this+0xee8/this+0xef0. The complete class-10 mutation surface of that whole chain is exactly TWO hardware ops: a blind unsaved writeReg(0x50, 00 20 00 00, 4) in clearDpIRQ(), and the getAndClearInterrupt(0x14->0x18, 9) W1C. Neither has an inverse in Apple's code. CORRECTIONS: evidence/2026-07-24-t6040-hpm-class10-host-transition.md's 'write address 0x14, length 9' is a mis-decode of the slot-+0xb38 virtual call and is withdrawn; the 0x55 logical bits 3 and 4 are transposed on the wire; Sol's Type10 vtable slot table is independently CONFIRMED correct. Method additions now reusable: PAC-diversifier-checked vtable decoding (__ZTV + 0x10 + slot; VA = 0xfffffe0007004000 + (raw & 0xffffffff)), slot +0xb08 = writeReg and +0xb10 = execute4Cc, and the 4CC command channel (codes are 32-bit immediates written to CMD1 = register 0x08, invisible to strings). RISK FRAMING UNCHANGED: this is a completeness verdict, not a danger claim; the exhaustive write inventory independently confirms no flash/OTP/non-volatile path in the class-10 surface, so both irreversible ops are volatile state cleared by a power cycle (see evidence/2026-07-25-t6040-r3-risk-calibration.md). Do not build or run 102-108 on this evidence. To close 096 still needs: primary evidence that a saved register-0x50 word can be safely written back (it has at least one W1C-style bit), a decided position on the 0x18 W1C, and either a validated cross-layer composition order or a design that needs none. Evidence: evidence/2026-07-25-t6040-hpm2-rollback-evidence.md (supersedes the per-item conclusions in evidence/2026-07-24-t6040-hpm2-detach-static-slice.md, whose slot table it confirms). NEW PRIMARY EVIDENCE 2026-07-25 (claude, maintainer-authorised while sol offline): found a matched operation/inverse pair. AppleHPMInterface::roleSwap(unsigned char) at VA 0xfffffe0009521fd0 issues exactly two TPS6598x 4CCs — roleSwap(0)=SWDF (swap to DFP/host), roleSwap(1)=SWUF (swap to UFP/device), arg>1 issues nothing; constants at VA 0xfffffe00074e3208/0x...3200 stored as 16-bit halfwords, hence the rev64.4h+uzp1.8b packing. This lives in the AppleHPMInterface* hierarchy, which is why the earlier AppleTCController*-focused slice missed it. Also: AppleHPMInterface::turnOnVbus() is a no-op stub (mov w0,#0; ret), confirming the slice. PROOF BY EXHAUSTION: scanning the entire 119 MB kernelcache for halfword-encoded 4CCs yields ONLY SWDF and SWUF — no SWSr/SWSk power-role swap, no issued GAID (only a 'Mode after GAID' log string), and NO flash/OTP commands anywhere, which both (a) strengthens 'no VBUS-off operation' from not-found to not-in-the-vocabulary, and (b) independently corroborates the no-persistent-brick risk calibration. Bonus: the string 'SSPS blocked due to forced usb device role and UFPf' shows a forced-UFP state can block SSPS. CONSEQUENCE: R3 should be redesigned around SWDF forward / SWUF inverse plus the decoded mode-flag teardown, NOT around a VBUS-off register. R3 REMAINS NO-GO: SWDF/SWUF are DATA-role commands, so whether VBUS drops on leaving DFP is unproven and must be measured; items 3-6 (mask/detect restoration, pre-SSPS 0x07, 0x23 inverse / neutral 0x55, teardown order) are still open. Evidence: evidence/2026-07-25-t6040-hpm2-roleswap-evidence.md.

## Former `note` field

2026-07-28 DECODED, and my 2026-07-26 'SWDF/SWUF do not exist' note was WRONG — I searched the wrong encoding (ASCII bytes + u32 immediate); they are UTF-16 string constants. AppleHPMInterface::roleSwap(unsigned char) @ VA 0xfffffe0009521fd0 loads, by role byte: role 0 -> 0xfffffe00074e3208 = 53005700440046 = UTF-16 'SWDF' (Swap to DFP = host/source, enables VBUS); role 1 -> 0xfffffe00074e3200 = UTF-16 'SWUF' (UFP/device); then rev64 and issue as a 4CC. So the original claim holds. Addresses script-decoded + segment-table checked. This UNBLOCKS an R3 spec: identical to R2's proven SSPS path in m1n1-hpm2 t6040_hpm2.c (write_logical_reg(CMD1 0x08, {'S','S','P','S'})) but with the 4CC = {'S','W','D','F'}. REMAINING before build: (1) trace roleSwap's own write to confirm the target register is CMD1 vs a per-port cmd reg (offline, same disasm); (2) byte-level review of the 4CC const (DFU-4CC caution); (3) attended run on the 096/097 SPMI framework. Cleaner long-term path is the SPMI Type-C stack (170). Evidence: evidence/2026-07-28-t6040-roleswap-decoded-swdf-swuf-confirmed.md. | 2026-07-28 STEP 1 DONE: roleSwap calls AppleHPMInterface::execute4Cc(uint16_t sel, uint8_t* cmd,...) @ 0xfffffe0009519774, whose selector arg is compared against #8 and #16 = CMD1(0x08)/CMD2(0x10). So the 4CC command register is CMD1 0x08 — exactly what the proven R2 SSPS path writes. R3 is now FULLY SPECIFIED with no further RE: R2's t6040_hpm2.c path with command[4]={'S','W','D','F'} to TPS_REG_CMD1 (0x08); SWUF is the device-role inverse for rollback. Remaining: byte-level review of the 4CC const + maintainer-attended run on the 096/097 SPMI framework. No knowledge gap left.

## Former `progress` field

 | 2026-07-29 CORRECTION: R3 was withdrawn as a VBUS test — SWDF is the DATA-role swap (DFP/UFP), not power-role, and ticket 176 then proved SWSr/SWSk do not exist in Apple's stack at all. The 'r3-fully-specified' state this ticket carried is obsolete.
