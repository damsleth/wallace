# T6040 right-HPM2 R0/R1/R2 candidate preflight

Date: 2026-07-24
Ticket: 092
Scope: source, captured ADT, clean builds, and linked-artifact inspection only

> **Final series amendment:** this file preserves the original candidate
> evolution and therefore contains historical eligibility statements. The
> actual narrowed live sequence is complete: ticket 093 found the inactive
> selector, ticket 094 sent WAKEUP and read `0x07`, and ticket 095 used exact
> m1n1 `276f4059d8c4` / binary `23737cd3...` to send only DATA1 `00` + CMD1
> `SSPS` and read final state `0x00`. The original IntMask R2 candidate below
> was never run. Current result:
> `evidence/2026-07-24-t6040-hpm2-r2-ssps-s0-result.md`.

No rig lease was acquired and no target, SPMI controller, USB-C manager, or
physical device was accessed.

## Result

Three separate direct-HPM2 m1n1 candidates are built. The first two live R0
artifacts each failed closed before SPMI on conservative identity-gate
representation mistakes. The current corrected artifact validates both raw
and translated controller addresses. Only its R0 may become eligible after
the exact independent review below; R1 and R2 remain dependency-gated.

Source:

```text
worktree: /Users/damsleth/Code/m1n1-hpm2
branch:   codex/t6040-hpm2-stages
commit:   471700035efd99e2e0bf4c3f5d6fbccaf07beb32
base:     c5b15c56 (endpoint-scoped AGENTS policy)
series:   e90b3ced (strict staged probes), 0c392103 (uncertain-write restore),
          d4f13a62 (raw strict RX logging),
          ef707f51 (volatile ADT fields excluded from identity enforcement),
          47170003 (raw and translated controller tuple validation)
```

Reproducer:

```text
scripts/t6040-build-hpm2-candidates.sh
```

Artifacts:

```text
/Users/damsleth/Code/linux-build-out/t6040-hpm2-471700035efd/
```

The script performs two clean builds per class and compares the BIN, Mach-O,
ELF, raw ELF, and sorted symbol list byte for byte. All comparisons passed.

| Class | `m1n1.bin` SHA-256 | Exact additional scope |
|---|---|---|
| R0 | `3a8af1e7daa65a431486f9cebbcad87f72995af5af601695ba93673710a2f01a` | selector plus one-byte logical `0x20` read |
| R1 | `284e50236a22f36538d1bec9ee296d186165f9b507189586db707d72737bd64a` | R0 semantics plus WAKEUP and conditional SSPS-to-S0 |
| R2 | `4637e56c46ef9ff81df5bdbdbbb0dbe4a6d7da27914e8a1bdd10da7e2298280f` | R1 prefix plus nine-byte IntMask1 save/zero/verify/restore/verify |

Full ELF hashes and symbol inventories are beside each binary.

## Runtime identity gate

The candidate runs only after the normal proven m1n1 baseline reaches
`pmgr_init()`. It then validates the live ADT before the first SPMI
transaction:

- exact size `606208`; the full-tree CRC is logged as provenance telemetry but
  is not fatal because the sanctioned chainloader rewrites volatile
  `/chosen/memory-map` handoff fields;
- the captured firmware ADT is mapped offline to SHA-256
  `7a92e6e4d16cb1b5a5858beb22b22acc8e5ed4b36ed5d5ccde9b251f1da55c84`;
- exact J614s/Mac16,8 root strings, compatible list, chip ID `0x6040`, and
  board ID `4`;
- exact `/arm-io/nub-spmi-a1`, compatible `aapl,spmi`, generation 3, complete
  raw 16 KiB `reg` tuples (`0x309198000`, `0x309194000`, `0x309190000`), and
  independently translated CPU physical banks (`0x509198000`,
  `0x509194000`, `0x509190000`);
- exactly one controller child, and that child is exact `hpm2`;
- exact SN201202x compatible, no child nodes, SID/register tuple
  `{0x0c,3,0,4,0,0}`, rid 2, class 10, port 3, and `right`.

Any mismatch prints a failure, performs zero SPMI transactions, and takes the
same intentional warm-reboot stop.

## Strict transport changes

The implementation starts from the public Gen3/Gen4 register-layout work but
does not import generic HPM iteration or the TPS initialization sequence.

- Direct path and SID only; no bus, HPM, or SID iteration.
- Every command checks TX-empty and refuses leftover RX before writing.
- RX wait is bounded to 1,000 × 10 us.
- The selector poll is bounded to 100 × 1 ms and accepts only the exact target
  or target-with-trigger-bit values.
- SSPS status is bounded to 1,000 × 100 us and rejects `!CMD`.
- Input/output sizes and null buffers are checked.
- Strict mode rejects trailing RX after the expected response.
- Every transaction logs SID, opcode, extra field, lengths, and write bytes.
  It logs the raw reply header and every consumed data word before validating
  ACK, parity, or trailing-RX state, so failed responses retain evidence.
- The pre-existing non-strict leftover-RX drain is now bounded to 64 words
  instead of an unbounded loop.

Yuka's allocation, double-shutdown, index-bound, and false-success issues are
avoided entirely: these candidates allocate no TPS object, call no generic
iterator, and always stop after one direct endpoint result.

## Per-class linked boundary

The linker uses function sections and garbage collection. Artifact inspection
proved:

- R0 links `spmi_init_strict`, `spmi_reg0_write`, and `spmi_ext_read`; it does
  not link WAKEUP or extended write.
- R1/R2 additionally link `spmi_send_wakeup` and `spmi_ext_write`.
- No candidate links `spmi_send_reset`, `spmi_send_sleep`,
  `spmi_send_shutdown`, TPS6598x code, generic USB initialization, USB PHY
  bring-up, or long SPMI transfers.
- The experiment path calls `flush_and_reboot()` immediately after PASS or
  FAIL, before display, payload, proxy, USB, PHY, or Linux paths.

The only build warnings are pre-existing m1n1 warnings in DCP I/O layout, an
MCC format string, and a Rust feature declaration. The new sources build
without a warning in all three configurations, and the default build without
an experiment macro also succeeds.

## Exact operation manifests

R0:

1. Register-0 Write/zero-write encoding selects logical `0x20`.
2. Bounded extended reads of physical selector `0x00`.
3. One-byte extended read of physical data window `0x20`.
4. Stop and warm reboot.

R1:

1. SPMI WAKEUP for SID `0x0c`, then 10 ms delay.
2. The same bounded logical power-state read.
3. Only if nonzero: logical DATA1 `0x09` write `{0x00}`, logical CMD1 `0x08`
   write `"SSPS"`, then bounded CMD1 polling.
4. Verify logical power state is zero.
5. Stop and warm reboot. No SLEEP or SHUTDOWN is used as cleanup.

R2:

1. The complete R1 prefix.
2. Read and save logical IntMask1 `0x16`, exactly nine bytes.
3. Write nine zero bytes (the existing m1n1 meaning of temporarily masking
   all HPM events), read back, and require equality.
4. Write back the original nine bytes, read back, and require equality.
5. From the first temporary-write attempt onward, assume the write may have
   landed even if its response is lost or malformed; every path then attempts
   the same original-byte restore.
6. Never read or write IntClear1/W1C event state.

The R2 zero-mask meaning and failure/restore behavior are specifically called
out for independent review. A warm reboot is recovery after the transcript;
it is not treated as proof that a failed mask restore was reversible.

## Independent review

The independent reviewer cloned exact commit `ef707f51f181` into a separate
temporary tree and reran the reproducer. Both clean builds per class compared
byte-identically and reproduced all three hashes above. The reviewer also
checked the ADT fail-closed gate, Gen3 and selector encodings, bounded waits,
linked-operation separation, raw-before-validation response logging, and R2's
zero-mask and uncertain-write restoration paths.

Result: **PASS**. No hardware was touched.

Residual non-blockers:

- runtime identity uses exact size and all endpoint-selecting properties, with
  the whole-tree CRC retained as telemetry and full SHA-256 checked offline;
- if the transport itself fails during an R2 restoration attempt, the
  candidate can only report `CRITICAL` and warm reboot; restoration cannot be
  guaranteed in that failure mode.

Ticket 092 can close. The user's manual plan approvals remain recorded for
093–095. Only R0/093 is eligible next; R1 and R2 remain data-dependent on the
preceding live pass.

## Initial-artifact cross-agent review — claude, 2026-07-24

This review applied to `d4f13a62` before its live full-tree CRC gate was shown
to reject the sanctioned chainloader's volatile handoff rewrites. It is
retained as historical source review; exact replacement review is recorded
below.

This was the COORDINATION.md cross-agent gate (non-author review) for **ticket
093 / R0**. I independently read the exact source at `d4f13a62` via git
(`src/t6040_hpm2.c`, `src/t6040_hpm2.h`, `src/main.c`, `src/spmi.c`) and traced
the full R0 call graph. **Verdict: PASS — 093 cleared for its one boot** (R1/R2
remain gated behind the preceding live pass).

Confirmed against `docs/SPMI_SAFETY.md` and the m1n1 non-negotiables:

- **R0 has no forbidden code, by construction.** For
  `T6040_HPM2_EXPERIMENT_CLASS == 0`, `write_logical_reg`, `command_ssps_s0`,
  `reach_s0`, and `mask_roundtrip` are all behind `#if …>=1/>=2` and are not
  compiled in. The linker-GC evidence above (R0 links only
  `spmi_init_strict`/`spmi_reg0_write`/`spmi_ext_read`; no WAKEUP/ext-write/
  reset/sleep/shutdown/TPS/USB/PHY) matches the source. The R0 call graph is
  exactly: `verify_adt` → `spmi_init_strict` → `read_power_state`
  (`select_logical_reg` reg0-selector + bounded selector poll, then one 1-byte
  `spmi_ext_read` of 0x20) → `spmi_shutdown`.
- **Fail-closed identity gate.** `verify_adt()` asserts exact ADT size+crc32,
  J614s/Mac16,8 root, chip 0x6040/board 4, sole-child `hpm2` under
  `nub-spmi-a1`, `aapl,spmi` gen 3, the three Gen3 bases (`0x309198000`/
  `…194000`/`…190000`), SN201202x, SID `0x0c`, rid 2, class 10, port 3, right.
  Any mismatch → "zero SPMI transactions", return -1. No blind MMIO — the
  controller base is ADT-derived, never swept.
- **Endpoint-scoped transport.** `spmi_init_strict` binds only the
  `nub-spmi-a1` reg[0] controller; every command carries SID `0x0c` from the
  caller; no bus/HPM/SID iteration; no PMU/Abbey/AOP controller reachable. The
  only "write" is the Register-0 selector (explicitly authorized for a reviewed
  status read; not a general write exemption).
- **Bounded + strict.** Selector poll 100×1 ms, RX wait 1000×10 us; strict mode
  refuses a dirty RX FIFO (093 "leftover FIFO" stop) and rejects trailing RX;
  reply opcode/addr/parity/ACK validated; raw reply logged before validation so
  a failed response keeps evidence.
- **Hard architectural stop.** `main.c` runs the experiment right after
  `pmgr_init()` — before DebugUSB/display/fb/SMP/payload/Linux — then
  `flush_and_reboot()` unconditionally. One read, log, warm reboot: no Linux
  boot, no storage/USB/PHY, no second proxy command.

Scope of my review vs. what I relied on: I performed the **source + design**
review conclusively (the safety-critical axis). For **build provenance** I
relied on this doc's documented two-build byte-identical reproduction + linker-GC
inspection; I did **not** independently rebuild for R0. Two recommendations:
(1) the live operator should confirm the runtime banner reads `class R0` (and
`intentional stop and warm reboot`) in the boot log before trusting the result;
(2) before **094 (R1 — the first SPMI *write*)**, I will independently rebuild
and reproduce the R1 hash, since build provenance matters most at the first
write. 094/095 also remain a separate maintainer go beyond the recorded plan
approval — the first hpm2 write is a new risk class.

## Replacement review after fail-closed attempt 1

The first live R0 attempt and its zero-transaction result are recorded in
`evidence/2026-07-24-t6040-hpm2-r0-attempt1.md`.

An independent reviewer then checked exact replacement commit
`ef707f51f18151608c19e4ec931756b953fe895d`. The reviewer confirmed from
`chainload.py` that SEPFW, BootArgs, and optionally preoslog handoff addresses
are intentionally rewritten before the same-size ADT is pushed. Treating the
whole-tree CRC as diagnostic-only is therefore appropriate. Exact fatal gates
still cover every field capable of selecting the controller or endpoint:
root identity, chip and board, bus path/name, sole child, generation and
compatible strings, all controller base/size tuples, HPM compatible/no-child
state, complete SID tuple, rid, class, port, and `right`.

The reviewer independently cloned `ef707f51`, ran the two-clean-build
reproducer, matched all three replacement BIN hashes above, validated all
other supplied artifact hashes, and reconfirmed the linked symbol boundaries.

Result: **PASS**. No hardware was touched.

Residual non-blocker: the whole-tree CRC is provenance telemetry rather than
identity enforcement. A future normalized subtree digest could strengthen
provenance but would not add endpoint-selection safety beyond the exact fatal
gates.

## Current address-gate replacement review

Attempt 2 and its zero-transaction diagnosis are recorded in
`evidence/2026-07-24-t6040-hpm2-r0-attempt2.md`.

An independent reviewer checked exact commit
`471700035efd99e2e0bf4c3f5d6fbccaf07beb32` and the current artifact directory.
The captured ADT and source both contain the complete raw controller tuples:

```text
(0x309198000, 0x4000)
(0x309194000, 0x4000)
(0x309190000, 0x4000)
```

Disassembly independently proves the translated comparisons against
`0x509198000`, `0x509194000`, and `0x509190000` before
`spmi_init_strict()`. All other endpoint-selecting fatal gates and the
R0/R1/R2 linked-operation separation remain intact. Stored hashes verify.
A fresh isolated clone reproduced the BIN, Mach-O, and symbol-list hashes for
all three classes, including R0 `3a8af1e7...`.

Result: **PASS**. No hardware was touched.

Reproducibility nuance: the fresh-clone debug ELF/raw-ELF hashes differ because
DWARF embeds the absolute checkout path. Each same-path double build is
byte-identical, and the cross-path objects match after stripping debug
sections. Deployable BIN/Mach-O code is path-independently reproducible.
